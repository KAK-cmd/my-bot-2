# excel_parser.py - с системой замен для сайта sttec
from datetime import datetime, timedelta
import re

class ПарсерРасписания:
    def __init__(self):
        self.группа = "ИС1-21/ ИС1-22"
        self.дни_недели = {
            0: "Понедельник",
            1: "Вторник", 
            2: "Среда",
            3: "Четверг",
            4: "Пятница",
            5: "Суббота",
            6: "Воскресенье"
        }
        
        # Парсер замен
        self.url = "https://menu.sttec.yar.ru/timetable/rasp_first.html"
        self.последние_замены = {}
        self.время_последнего_обновления = None
        self.дата_замен = None
        self.группа_поиск = ["ИС1-21", "ИС1-22"]
        
        # Загружаем статическое расписание
        self.загрузить_расписание()
    
    def загрузить_расписание(self):
        """Загружает статическое расписание с чередованием по неделям"""
        self.расписание = {
            "Понедельник": {
                "числитель": [
                    {"номер": 0, "дисциплина": "МДК 06.02 Инженерно-техническая поддержка сопровождения ИС п/гр1", "преподаватель": "Юров А.А.", "аудитория": "Б302"},
                    {"номер": 1, "дисциплина": "Основы алгоритмизации и программирования", "преподаватель": "Королева П.Д.", "аудитория": "Б401"},
                    {"номер": 2, "дисциплина": "МДК 06.02 Инженерно-техническая поддержка сопровождения ИС", "преподаватель": "Юров А.А.", "аудитория": "Б202"},
                    {"номер": 3, "дисциплина": "Физическая культура", "преподаватель": "Куликова А.А.", "аудитория": "Сп.зал"}
                ],
                "знаменатель": [
                    {"номер": 0, "дисциплина": "МДК 06.02 Инженерно-техническая поддержка сопровождения ИС п/гр1", "преподаватель": "Юров А.А.", "аудитория": "Б302"},
                    {"номер": 1, "дисциплина": "Численные методы", "преподаватель": "Комиссарова О.В.", "аудитория": "А407"},
                    {"номер": 2, "дисциплина": "МДК 06.02 Инженерно-техническая поддержка сопровождения ИС", "преподаватель": "Юров А.А.", "аудитория": "Б202"}
                ]
            },
            "Вторник": {
                "числитель": [
                    {"номер": 1, "дисциплина": "МДК 06.02 Инженерно-техническая поддержка сопровождения ИС п/гр2", "преподаватель": "Панасюк А.Д.", "аудитория": "Б406"},
                    {"номер": 2, "дисциплина": "МДК 09.01 Проектирование и разработка веб-приложений", "преподаватель": "Юров А.А., Байдина Ю.А.", "аудитория": "Б301, Б302"},
                    {"номер": 3, "дисциплина": "Элементы высшей математики", "преподаватель": "Шереметьева Н.В.", "аудитория": "М106"}
                ],
                "знаменатель": [
                    {"номер": 0, "дисциплина": "МДК 06.02 Инженерно-техническая поддержка сопровождения ИС п/гр2", "преподаватель": "Панасюк А.Д.", "аудитория": "Б406"},
                    {"номер": 1, "дисциплина": "Численные методы", "преподаватель": "Комиссарова О.В., Садилова К.А.", "аудитория": "А407, А409"},
                    {"номер": 2, "дисциплина": "МДК 09.01 Проектирование и разработка веб-приложений", "преподаватель": "Юров А.А., Байдина Ю.А.", "аудитория": "Б301, Б302"},
                    {"номер": 3, "дисциплина": "Элементы высшей математики", "преподаватель": "Шереметьева Н.В.", "аудитория": "М106"}
                ]
            },
            "Среда": {
                "числитель": [
                    {"номер": 0, "дисциплина": "МДК 08.02 Графический дизайн и мультимедиа", "преподаватель": "Вершинина Н.А., Серова А.М.", "аудитория": "Б301, Б309"},
                    {"номер": 1, "дисциплина": "МДК 02.02 Инструментальные средства разработки ПО", "преподаватель": "Байдина Ю.А., Пронина Л.Ю.", "аудитория": "Б302, Б305"},
                    {"номер": 2, "дисциплина": "УП 02 Учебная практика", "преподаватель": "Пронина Л.Ю., Кувшинова В.А.", "аудитория": "Б305, Б309"}
                ],
                "знаменатель": [
                    {"номер": 0, "дисциплина": "МДК 08.02 Графический дизайн и мультимедиа", "преподаватель": "Вершинина Н.А., Серова А.М.", "аудитория": "Б301, Б309"},
                    {"номер": 1, "дисциплина": "МДК 06.03 Устройства и функционирование ИС", "преподаватель": "Пронина Л.Ю., Байдина Ю.А.", "аудитория": "Б305, Б302"},
                    {"номер": 2, "дисциплина": "УП 02 Учебная практика", "преподаватель": "Пронина Л.Ю., Кувшинова В.А.", "аудитория": "Б305, Б309"}
                ]
            },
            "Четверг": {
                "числитель": [
                    {"номер": 1, "дисциплина": "Основы проектирования БД", "преподаватель": "Гудкова А.Л.", "аудитория": "ДОТ (онлайн)"},
                    {"номер": 2, "дисциплина": "Элементы высшей математики", "преподаватель": "Шереметьева Н.В.", "аудитория": "ДОТ (онлайн)"},
                    {"номер": 3, "дисциплина": "МДК 02.02 Инструментальные средства разработки ПО", "преподаватель": "Байдина Ю.А.", "аудитория": "ДОТ (онлайн)"},
                    {"номер": 4, "дисциплина": "МДК 06.03 Устройства и функционирование ИС", "преподаватель": "Пронина Л.Ю.", "аудитория": "ДОТ (онлайн)"}
                ],
                "знаменатель": [
                    {"номер": 1, "дисциплина": "МДК 08.01 Проектирование и разработка интерфейсов пользователя", "преподаватель": "Гудкова А.Л.", "аудитория": "ДОТ (онлайн)"},
                    {"номер": 2, "дисциплина": "МДК 09.01 Проектирование и разработка веб-приложений", "преподаватель": "Юров А.А.", "аудитория": "ДОТ (онлайн)"},
                    {"номер": 3, "дисциплина": "МДК 08.02 Графический дизайн и мультимедиа", "преподаватель": "Вершинина Н.А.", "аудитория": "ДОТ (онлайн)"},
                    {"номер": 4, "дисциплина": "МДК 06.03 Устройства и функционирование ИС", "преподаватель": "Пронина Л.Ю.", "аудитория": "ДОТ (онлайн)"}
                ]
            },
            "Пятница": {
                "числитель": [
                    {"номер": 1, "дисциплина": "Основы проектирования БД", "преподаватель": "Гудкова А.Л., Серова А.М.", "аудитория": "Б301, Б302"},
                    {"номер": 2, "дисциплина": "МДК 09.02 Оптимизация веб-приложений", "преподаватель": "Гудкова А.Л., Серова А.М.", "аудитория": "Б301, Б305"},
                    {"номер": 3, "дисциплина": "МДК 08.01 Проектирование и разработка интерфейсов пользователя", "преподаватель": "Гудкова А.Л., Серова А.М.", "аудитория": "Б301, Б406"}
                ],
                "знаменатель": [
                    {"номер": 1, "дисциплина": "Основы проектирования БД", "преподаватель": "Гудкова А.Л., Серова А.М.", "аудитория": "Б301, Б302"},
                    {"номер": 2, "дисциплина": "МДК 09.02 Оптимизация веб-приложений", "преподаватель": "Гудкова А.Л., Серова А.М.", "аудитория": "Б301, Б305"},
                    {"номер": 3, "дисциплина": "МДК 08.01 Проектирование и разработка интерфейсов пользователя", "преподаватель": "Гудкова А.Л., Серова А.М.", "аудитория": "Б301, Б406"}
                ]
            },
            "Суббота": {
                "числитель": [
                    {"номер": 0, "дисциплина": "Иностранный язык в профессиональной деятельности", "преподаватель": "Зубковская А.Е., Загородних Е.С.", "аудитория": "А413, А404-3"},
                    {"номер": 1, "дисциплина": "Основы алгоритмизации и программирования", "преподаватель": "Королева П.Д., Неделяева Н.А.", "аудитория": "Б301, Б302"}
                ],
                "знаменатель": [
                    {"номер": 0, "дисциплина": "Иностранный язык в профессиональной деятельности", "преподаватель": "Зубковская А.Е., Загородних Е.С.", "аудитория": "А413, А404-3"},
                    {"номер": 1, "дисциплина": "Основы алгоритмизации и программирования", "преподаватель": "Королева П.Д., Неделяева Н.А.", "аудитория": "Б301, Б302"}
                ]
            },
            "Воскресенье": {
                "числитель": [],
                "знаменатель": []
            }
        }
        print("✅ Расписание загружено")
    
    async def обновить_замены(self):
        """Обновляет замены с сайта sttec"""
        import aiohttp
        try:
            async with aiohttp.ClientSession() as session:
                async with session.get(self.url, timeout=10) as response:
                    if response.status == 200:
                        html = await response.text()
                        замены, дата_замен = self.распарсить_замены(html)
                        self.последние_замены = замены
                        self.дата_замен = дата_замен
                        self.время_последнего_обновления = datetime.now()
                        print(f"✅ Замены обновлены для {дата_замен}: {len(замены)} записей")
                        return True
                    else:
                        print(f"❌ Ошибка HTTP: {response.status}")
                        return False
        except Exception as e:
            print(f"❌ Ошибка при получении замен: {e}")
            return False
    
    def распарсить_замены(self, html):
        """Парсит HTML и извлекает замены для группы ИС1-21/ИС1-22"""
        замены = {}
        
        # Извлекаем дату из заголовка
        дата_match = re.search(r'на (\d{1,2} \w+ \d{4}) года', html)
        дата_замен = дата_match.group(1) if дата_match else "Неизвестно"
        
        # Ищем строки с нашей группой
        lines = html.split('\n')
        в_таблице = False
        
        for line in lines:
            # Начинаем парсинг после заголовка таблицы
            if '№\tГруппа\tНомер\t' in line:
                в_таблице = True
                continue
            
            if в_таблице:
                # Проверяем, не кончилась ли таблица
                if 'Методист учебного отдела' in line:
                    break
                
                # Разделяем по табуляциям или нескольким пробелам
                parts = re.split(r'\t|\s{2,}', line.strip())
                if len(parts) >= 5:
                    группа = parts[1]
                    
                    # Проверяем нашу группу
                    if any(гр in группа for гр in self.группа_поиск):
                        номер_пары = parts[2].strip()
                        старая_дисциплина = parts[3].strip() if len(parts) > 3 else ""
                        новая_дисциплина = parts[4].strip() if len(parts) > 4 else ""
                        аудитория = parts[5].strip() if len(parts) > 5 else ""
                        
                        # Обрабатываем несколько пар через запятую
                        номера_пар = номер_пары.split(',')
                        
                        for номер in номера_пар:
                            if номер.strip():
                                ключ = номер.strip()
                                замены[ключ] = {
                                    'старая_дисциплина': старая_дисциплина,
                                    'новая_дисциплина': новая_дисциплина,
                                    'аудитория': аудитория,
                                    'группа': группа,
                                    'снято': 'снято' in новая_дисциплина.lower() or 'снято' in аудитория.lower()
                                }
        
        return замены, дата_замен
    
    def есть_замены(self):
        """Проверяет, есть ли замены для нашей группы"""
        return len(self.последние_замены) > 0
    
    def получить_текст_замен(self):
        """Форматирует замены в текст"""
        if not self.последние_замены:
            return "📝 На сегодня замен нет"
        
        текст = f"🔄 **ЗАМЕНЫ НА {self.дата_замен.upper()}**\n\n"
        
        # Сортируем по номеру пары
        сортированные_замены = sorted(self.последние_замены.items(), 
                                      key=lambda x: int(x[0]) if x[0].isdigit() else 99)
        
        for номер_пары, данные in сортированные_замены:
            if данные['снято']:
                текст += f"❌ **{номер_пары} пара - СНЯТА**\n"
                if данные['старая_дисциплина'] and данные['старая_дисциплина'] != "...":
                    текст += f"   📖 Было: {данные['старая_дисциплина']}\n"
                текст += "\n"
            else:
                текст += f"🔄 **{номер_пары} пара**\n"
                if данные['старая_дисциплина'] and данные['старая_дисциплина'] != "...":
                    текст += f"   📖 Вместо: {данные['старая_дисциплина']}\n"
                текст += f"   ✅ Будет: {данные['новая_дисциплина']}\n"
                if данные['аудитория'] and данные['аудитория'].lower() != 'снято':
                    текст += f"   🏫 Аудитория: {данные['аудитория']}\n"
                текст += "\n"
        
        if self.время_последнего_обновления:
            время = self.время_последнего_обновления.strftime('%H:%M')
            текст += f"🕐 Обновлено: {время}"
        
        return текст
    
    def получить_расписание_с_заменами(self, день_недели=None, для_даты=None):
        """Возвращает расписание с учетом замен"""
        if день_недели is None:
            день_недели = self.получить_сегодняшний_день()
        
        # Получаем обычное расписание
        расписание_текст = self.получить_расписание_на_день(день_недели, с_заменами=True, для_даты=для_даты)
        
        # Добавляем заголовок с заменами, если они есть
        if self.есть_замены():
            текст_замен = self.получить_текст_замен()
            расписание_текст = f"⚠️ **ВНИМАНИЕ: ЕСТЬ ЗАМЕНЫ!**\n\n{текст_замен}\n\n{'='*40}\n\n{расписание_текст}"
        
        return расписание_текст
    
    def получить_расписание_на_день(self, день_недели=None, с_заменами=False, для_даты=None):
        """Возвращает расписание на день с учетом замен"""
        if день_недели is None:
            день_недели = self.получить_сегодняшний_день()
        
        # Определяем дату для отображения
        if для_даты:
            дата_отображения = для_даты
        else:
            дата_отображения = datetime.now()
        
        # Определяем неделю для выбранного дня
        # Если дата указана, используем ее для определения недели
        if для_даты:
            номер_недели_в_году = для_даты.isocalendar()[1]
        else:
            номер_недели_в_году = datetime.now().isocalendar()[1]
        
        if номер_недели_в_году % 2 == 0:
            текущая_неделя = "знаменатель"  # четная неделя
        else:
            текущая_неделя = "числитель"    # нечетная неделя
        
        # Проверяем есть ли расписание для этого дня
        if день_недели in self.расписание:
            день_данные = self.расписание[день_недели]
            
            # Сортируем пары по номеру
            пары = день_данные.get(текущая_неделя, [])
            
            # Применяем замены к парам если нужно
            if с_заменами:
                пары = self.применить_замены_к_парам(пары)
            
            пары_отсортированные = sorted(пары, key=lambda x: x["номер"])
            
            # Формируем текст расписания
            эмодзи_недели = "🔴" if текущая_неделя == "знаменатель" else "🔵"
            текст = f"{эмодзи_недели} {день_недели.upper()} ({текущая_неделя.upper()}) - {self.группа}\n"
            текст += f"📅 {дата_отображения.strftime('%d.%m.%Y')}\n\n"
            
            if not пары_отсортированные:
                текст += "🎉 Выходной! Пар нет.\n"
                if день_недели == "Воскресенье":
                    текст += "💤 Воскресенье - выходной день"
                else:
                    текст += "😴 Отдыхайте!"
            else:
                for пара in пары_отсортированные:
                    # Проверяем, была ли замена
                    if пара.get('замена', False):
                        if пара.get('снято', False):
                            эмодзи_пары = "❌"  # Пара снята
                            примечание = " [СНЯТА]"
                        else:
                            эмодзи_пары = "🔄"  # Эмодзи для замены
                            примечание = " [ЗАМЕНА]"
                    else:
                        эмодзи_пары = "🔴" if текущая_неделя == "знаменатель" else "🔵"
                        примечание = ""
                    
                    текст += f"{эмодзи_пары} {пара['номер']} пара: {пара['дисциплина']}{примечание}\n"
                    текст += f"   👨‍🏫 {пара['преподаватель']}\n"
                    текст += f"   🏫 {пара['аудитория']}\n\n"
            
            return текст
        else:
            неделя_текст = "числитель" if текущая_неделя == "числитель" else "знаменатель"
            return f"📅 {день_недели.upper()} ({неделя_текст}) - {self.группа}\n\nРасписание не найдено"
    
    def применить_замены_к_парам(self, пары):
        """Применяет замены к списку пар"""
        if not self.последние_замены:
            return пары
        
        обновленные_пары = []
        примененные_замены = set()
        
        for пара in пары:
            номер_пары = str(пара['номер'])
            
            if номер_пары in self.последние_замены:
                # Помечаем как примененную замену
                примененные_замены.add(номер_пары)
                
                замена = self.последние_замены[номер_пары]
                
                if замена['снято']:
                    # Пара снята - не добавляем в расписание
                    continue
                else:
                    # Создаем новую пару с заменой
                    новая_пара = пара.copy()
                    новая_пара['дисциплина'] = замена['новая_дисциплина']
                    новая_пара['замена'] = True
                    новая_пара['снято'] = False
                    
                    # Обновляем аудиторию, если указана в замене
                    if замена['аудитория'] and замена['аудитория'].lower() != 'снято':
                        новая_пара['аудитория'] = замена['аудитория']
                    
                    обновленные_пары.append(новая_пара)
            else:
                # Без изменений
                обновленные_пары.append(пара)
        
        # Добавляем новые пары, которых не было в оригинальном расписании
        for номер_пары, замена in self.последние_замены.items():
            if номер_пары not in примененные_замены and not замена['снято']:
                # Это новая пара (добавленная замена)
                новая_пара = {
                    'номер': int(номер_пары),
                    'дисциплина': замена['новая_дисциплина'],
                    'преподаватель': '',  # Извлекаем из названия если есть
                    'аудитория': замена['аудитория'] if замена['аудитория'] and замена['аудитория'].lower() != 'снято' else '',
                    'замена': True,
                    'снято': False,
                    'новая_пара': True
                }
                
                # Пытаемся извлечь преподавателя из названия
                if '(' in замена['новая_дисциплина']:
                    parts = замена['новая_дисциплина'].split('(')
                    if len(parts) > 1:
                        новая_пара['преподаватель'] = parts[-1].replace(')', '').strip()
                        новая_пара['дисциплина'] = parts[0].strip()
                
                обновленные_пары.append(новая_пара)
        
        # Сортируем по номеру пары
        обновленные_пары.sort(key=lambda x: x['номер'])
        
        return обновленные_пары
    
    def получить_текущую_неделю(self):
        """Определяет числитель или знаменатель"""
        сегодня = datetime.now()
        номер_недели_в_году = сегодня.isocalendar()[1]
        
        if номер_недели_в_году % 2 == 0:
            return "знаменатель"  # четная неделя
        else:
            return "числитель"    # нечетная неделя
    
    def получить_сегодняшний_день(self):
        """Возвращает название сегодняшнего дня недели"""
        сегодня_номер = datetime.now().weekday()
        return self.дни_недели.get(сегодня_номер, "Неизвестно")
    
    def получить_информацию_о_неделе(self):
        """Возвращает информацию о текущей неделе"""
        текущая_неделя = self.получить_текущую_неделю()
        сегодня = datetime.now()
        номер_недели_в_году = сегодня.isocalendar()[1]
        
        эмодзи_недели = "🔴" if текущая_неделя == "знаменатель" else "🔵"
        текст = f"{эмодзи_недели} ИНФОРМАЦИЯ О НЕДЕЛЕ\n\n"
        текст += f"📅 Дата: {сегодня.strftime('%d.%m.%Y')}\n"
        текст += f"📅 День недели: {self.получить_сегодняшний_день()}\n"
        текст += f"📊 Неделя года: {номер_недели_в_году}\n"
        текст += f"🎯 Сейчас идет: {эмодзи_недели} {текущая_неделя.upper()}\n\n"
        
        if текущая_неделя == "числитель":
            текст += "🔵 **ЧИСЛИТЕЛЬ** (первая неделя)\n"
            текст += "• Понедельник (1 пара): Основы алгоритмизации и программирования\n"
            текст += "• Понедельник (3 пара): Физическая культура (есть)\n"
            текст += "• Вторник (0 пара): нет пары\n"
            текст += "• Вторник (1 пара): МДК 06.02 (п/гр2)\n"
            текст += "• Среда (1 пара): МДК 02.02 Инструментальные средства\n"
            текст += "• Четверг (1 пара): Основы проектирования БД\n"
            текст += "• Четверг (2 пара): Элементы высшей математики\n"
            текст += "• Четверг (3 пара): МДК 02.02 Инструментальные средства\n"
        else:
            текст += "🔴 **ЗНАМЕНАТЕЛЬ** (вторая неделя)\n"
            текст += "• Понедельник (1 пара): Численные методы\n"
            текст += "• Понедельник (3 пара): Физическая культура (нет)\n"
            текст += "• Вторник (0 пара): МДК 06.02 (п/гр2)\n"
            текст += "• Вторник (1 пара): Численные методы\n"
            текст += "• Среда (1 пара): МДК 06.03 Устройства и функционирование ИС\n"
            текст += "• Четверг (1 пара): МДК 08.01 Проектирование интерфейсов\n"
            текст += "• Четверг (2 пара): МДК 09.01 Проектирование веб-приложений\n"
            текст += "• Четверг (3 пара): МДК 08.02 Графический дизайн и мультимедиа\n"
        
        следующая_неделя = "числитель" if текущая_неделя == "знаменатель" else "знаменатель"
        эмодзи_след = "🔵" if следующая_неделя == "числитель" else "🔴"
        текст += f"\n💡 Следующая неделя будет: {эмодзи_след} {следующая_неделя.upper()}"
        
        return текст
    
    def получить_расписание_принудительно(self, день_недели, неделя):
        """Показывает расписание для конкретной недели"""
        if день_недели in self.расписание:
            день_данные = self.расписание[день_недели]
            
            пары = день_данные.get(неделя, [])
            пары_отсортированные = sorted(пары, key=lambda x: x["номер"])
            
            эмодзи_недели = "🔴" if неделя == "знаменатель" else "🔵"
            текст = f"{эмодзи_недели} {день_недели.upper()} ({неделя.upper()}) - {self.группа}\n\n"
            
            if not пары_отсортированные:
                текст += "🎉 Выходной! Пар нет.\n"
                if день_недели == "Воскресенье":
                    текст += "💤 Воскресенье - выходной день"
            else:
                for пара in пары_отсортированные:
                    эмодзи_пары = "🔴" if неделя == "знаменатель" else "🔵"
                    текст += f"{эмодзи_пары} {пара['номер']} пара: {пара['дисциплина']}\n"
                    текст += f"   👨‍🏫 {пара['преподаватель']}\n"
                    текст += f"   🏫 {пара['аудитория']}\n\n"
            
            текст += f"💡 Показано принудительно для: {неделя.upper()}"
            return текст
        else:
            return f"📅 {день_недели.upper()} ({неделя}) - {self.группа}\n\nРасписание не найдено"
    
    def получить_дату_завтра(self):
        """Возвращает дату завтрашнего дня"""
        завтра = datetime.now() + timedelta(days=1)
        return завтра